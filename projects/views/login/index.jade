extends ../layout

block content
	style(type='text/css').
		.error {
			color: red;
		}

	div(class="container")
		h2.
			#{title}

		p(class="error") #{error}

		form#formLogin(name="login", method="post", action="/login")
			div(class="form-group row")
				p(for=email) E-mail:
					input#inputEmail(type="email", placeholder="E-mail", name="email", class="form-control", value=data.email, required)
			div(class="form-group row")
				p(for=password) Zaporka:
					input#inputPassword(type="password", placeholder="Zaporka", name="password", class="form-control", value=data.password, required)
				p
					button#btnSubmit(type="submit", class="btn btn-primary") Prijavi se
		p Nemaš korisnički račun? Registriraj se
		form(action='/register', method='get')
				button(type='submit' class="btn btn-primary").
					Registracija korisnika


